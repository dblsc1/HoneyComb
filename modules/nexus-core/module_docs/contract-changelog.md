# nexus-core · 契约变更记录

> 从 `contract.md` 拆出（2026-08-02，主文件触到单文件 500 行上限）。
>
> **拆的是历史，不是规范。** 规范只有一处：`contract.md`。
> 本文件只回答「什么时候为什么改的」，**不承载任何当前有效的约束**——
> 想知道现在的规则是什么，看 `contract.md`，别在这里找。
>
> 这么拆是为了避免今天反复踩的那个坑：同一条规则两份副本、改一处忘另一处。
> 历史是 append-only 的，天然不会与规范打架。

## 变更记录

| 日期 | CR | 变更 |
|---|---|---|
| 2026-07-28 | 人类裁决「走 C，契约先行但只先行两个读端」 | v0.1 首版：固化 `views/current` 与 `views/tree` 两条读端响应模型；钉死占比为 0–100 百分数；划定内部子边界＝将来分拆线 |
| 2026-07-30 | 切片 2 开工 | v0.4：补 planner CRUD 十二条端点与三类对象响应形状；删除拒绝级联返 409；补齐分区/项目的 key 格式（v0.3 缺，programmer 已停下上报）；API 表状态列按切片 1 实况更新；标注 `views` 响应待补 `key` 字段 |
| 2026-07-30 | 人类裁决 J10（推翻 D9） | v0.3：标识三分 `id`/`key`/`name`；事件 subject 只存 opaque id；key 由「名字→号」登记表生成、不做音译、不做唯一约束；建任务时 id/key 系统生成 |
| 2026-07-30 | 人类裁决「业务优先，开切片 1」 | v0.2：补写入侧 `POST /events`、`POST /timer/start\|stop` 与 planner 只读面。四条规范性语义（防重按 dedupeKey、重复不算错误、recordedAt 服务端盖章、部分失败不整批回滚）；信封本身不复述，指向 `contracts/yq-event-v1.md` |
| 2026-08-01 | 人类裁决：上统一 CRUD 入口，走 join 不拆服务 | v0.5：新增 `/api/core/planner/{type}` 四条端点，十二条旧端点保留不删。钉死「路由归一、校验不归一」——错误消息点名字段是消费方依赖的契约保证，通用层会毁掉它。同批记下「为什么不拆独立服务」的完归并由（join 跨网络 + 分库没法 join），免得重新讨论 |
| 2026-08-01 | 人类裁决：旧端口清掉 + 档案读可加 | v0.6：**删除十二条 planner 旧端点**，统一入口成为唯一写路径（v0.5 的新旧并存是过渡不是设计——同一件事两个入口迟早有人只改一边）；**新增 `GET /api/core/events` 档案读端**，只读、不碰写侧与 DISPATCH、不在后端 join 名字 |
| 2026-08-01 | ring 实现者指出、复核采纳 | v0.7：把 `{detail: string}` 的 4xx 错误形状**写进契约**。此前它只是 exception handler 的观察行为，而 ring 与 table 都原样展示 `detail` 给用户——形状一改两个前端同时静默失败且无契约违反可抓。没写进契约的依赖就是没人守的依赖 |
| 2026-08-08 | PRD cockpit-v1 F-API-1..5 | v1.1：`TaskOut` 增 `plan`（复用项目 plan 校验）与 `dependsOn`（默认 `[]`，校验存在性/自指/成环 DFS）；删除被依赖的任务返 409 并指名依赖者；明文排期冲突与越界排期不校验（用户裁决）；O1——`GET /views/gantt` 补任务层，**扩现有 `proj_daily_stats` 读端，不新开投影**（taskId 从 v0.8 起就在文档里，只是读端按 project 聚合丢了任务粒度），因此本版**不需要跑投影重建**；补记此前遗漏的 `nexus-core.views.gantt.v1` provides 索引项；消费方索引补 `gantt` 并记录三方均为纯增量升级 |
| 2026-08-08 | 回收复核发现集成缝 | v1.2：`views.tree.v1` 的 `TaskNode` 补 `plan`/`dependsOn`（只读附带，与 `TaskOut` 同形状）——v1.1 只顾上了 `TaskOut` 与 `views.gantt.v1`，漏了 `views.tree.v1`，而 `ring`/`table` 实际读任务列表主要经过 tree，联调时 table 的 feature-detect 控件因此永远不亮。纯 additive，零迁移期；补记「与其他两条读端的对照」表防止再漏第三处 |
| 2026-08-08 | 收尾：追平消费方反向索引 | 无 schema 变更，纯索引记账：`ring`（契约 commit `2cba2aa`）与 `table`（契约 commit `603a44e`）已在各自模块契约里登记消费 `views.gantt.v1`，本仓消费方表补上这两行（含具体字段：`ring` 读 `today`/`projects[].id`/`projects[].tasks[].{id,name}`/`tasks[].actual[].{date,seconds}`；`table` 读 `projects[].plan`/`projects[].actual[].{date,seconds}`/`today`），并顺带补 `ring` 对 `timer.v1`（`POST /api/core/timer/cancel`）的消费——反向表已用端点/字段粒度记录，这条之前漏了 |
| 2026-08-09 | 只读数据导出 | v1.3：新增 `nexus-core.views.export.v1`（`GET /api/core/export`）——聚合 zones/projects/tasks 原始文档、events 原样全量、`proj_current`/`proj_daily_stats` 两张投影，外加服务端 `exportedAt`。纯只读聚合既有 service 层函数（`iter_all_events`/`list_zones`/`list_projects`/`list_tasks`/投影 handler 的 `read_*`），零直连 mongo、零新数据、零改动任何写路径；消费方 `table` 前端的「导出数据」按钮登记进消费方表 |
| 2026-08-09 | table agent 核实的缺口 | v1.4：`PATCH /api/core/planner/tasks/{id}` 补 `plannedWeight?`——`TaskUpdate`（`schemas.py`）当年切片 2 只顾上了 `TaskCreate`，没跟 `ProjectUpdate` 对称，PATCH 任务权重会被 `extra="forbid"` 422 拒收。`service.update_task` 接上既有 `_check_weight`（与 `update_project` 共用同一函数，非新校验），负数走既有 400 语义。`unified_router.py` 核实**不需要改**——它对 `TaskUpdate` 走 `body.model_dump(exclude_unset=True)` 直通，不逐字段映射。纯 additive，零迁移期 |
| 2026-08-10 | GTD 大版本 `gtd-v1` PRD 波1 | v1.5：新增 `nexus-core.views.next-actions.v1`（`GET /api/core/views/next-actions`，F-TODO-1）——可做/等待分类、过期琥珀标、按 zone 分组、按 plannedWeight 降序+今天到期/过期置顶排序，**图级环检测（三色 DFS + 深度上限）防历史脏数据死循环**（A8d，撞环任务降级为可做+警示标）；新增 `nexus-core.views.review.v1`（`GET /api/core/views/review`，F-REVIEW-1）——本周计划vs事实/过期项目/久未动任务/inbox 待清空计数四块聚合，全部复用既有只读函数，零新集合零新投影；`planner.crud.v1` 新增 `actor`（human/ai，缺省 human）写入参 + `lastWriter` 响应字段（F-ACTOR-1，**仅字段落库，来源区分/审计流水/F-API-3 设防明确留波2**，依赖尚未创建的 `ai-planner` 模块受控层）；well-known `p_inbox` 项目 + `z_inbox` 分区幂等种子创建，`p_inbox` 禁删返 409（F-INBOX-1/F-API-4，复用既有 `HasChildrenError`→409 映射，触发条件改为"系统单例"而非"有子对象"）。消费方表补 `table`/新 todo 前端与尚未创建的 `ai-planner` 模块的待登记行 |

| 2026-08-10 | GTD 大版本 `gtd-v1` PRD 波2-C（nexus 侧 AI 设防） | v1.6：补上 v1.5 明文欠下的三笔。① **actor 来源区分**（F-ACTOR-1 可信版本）：新增来源凭据头 `X-Nexus-Client-Token`，服务端据此把调用方判成 `ai`/`human`/`unverified` 三类；**来源压过请求体自报**——带 AI 凭据却自报 `actor:"human"` 直接 403，而"自称 ai"谁说都信（降权声明无法提权，这条不对称是威胁模型本身的形状）；头存在但不匹配任何凭据 **fail-closed 403，不降级成 unverified**；`unverified` 保持 v1.5 语义放行以免打死尚未携带凭据的前端，缺口登记在案并由 `NEXUS_ACTOR_STRICT`（默认 0）提供开关。② **F-API-3 高风险二次设防**：`DELETE` 任意 type、`PATCH` 含 `projectId`/`zoneId`/`plan`（含 `plan:null` 清空）判为高风险，**有效 actor 为 `ai` 时一律 403**，且拒绝发生在任何库写入与存在性检查之前（先拒来源再谈对象在不在，否则 404/409 差异会变成 id 探测器）；判据看 PATCH 请求体里**实际出现的键**而不是值有没有变——"有没有伸手"比"伸手后有没有变"更好判、更难绕，且后者要先读旧值、读写之间有竞态。③ **F-ACTOR-2 审计流水**：新增 `nexus-core.planner.audit.v1`——独立集合 `planner_audit`（**与 `events` 零交集**，不进信封/投影/重建/导出）+ 只读端点 `GET /api/core/planner/audit`；记录 `seq`（`counters` 原子自增，全序追溯不靠时间戳）/`at`/`actor`/`source`/`op`/`objectType`/`objectId`/`highRisk`/`outcome`/`changes`；**applied/denied/failed 三种 outcome 都记**——被拒的尝试是安全信号，failed 标出批量写崩在第几步；append-only 由「审计集合上只允许 insert_one/find/create_index/count_documents」的 AST 断言机械守住。④ `GET /api/core/health` 增 `actorGuard` 字段（`strict`/`lenient`），让设防姿态跨进程可判，同 v1.0 加 `db` 的理由。⑤ 配置表新增 `NEXUS_AI_CLIENT_TOKEN`/`NEXUS_HUMAN_CLIENT_TOKEN`/`NEXUS_ACTOR_STRICT`（严格模式缺人路径凭据 = 启动即失败，不许炸在用户点删除那一刻）。消费方表：`ai-planner` 行改为已落地并追加携带凭据的要求，新增 `nginx-docker` 网关注入人路径凭据的待办行（跨模块，须排期）。**`events` 台账语义一个字节未动** |
| 2026-08-11 | 复核发现规范空白（正文漏写判据） | v1.6 补丁（不改行为，纠正文档漏写）：「一、来源判定」表 `unverified` 判据从「头不存在」补全为「头不存在，或头存在但为空串/纯空白」，与实现 `planner/guard.py::resolve_source`（`token = (raw or "").strip()`）及测试 `test_empty_header_counts_as_absent` 对齐。理由写进正文：网关用 `proxy_set_header X-Nexus-Client-Token "$var"` 转发时，`$var` 未设发的正是空串，判成"带了个错凭据"会在网关配置疏漏时把整个前端写路径 403 打死——那是运维事故，不是攻击。此前该判据只活在代码注释与测试文档字符串里，契约正文没写，属规范空白（有外部消费方时，契约正文是唯一事实），不是新增行为，故不单独 bump 版本号 |
| 2026-08-12 | JSON 导入编辑接口 | v1.7：新增 `nexus-core.planner.import.v1`（`POST /api/core/import`）——「导出 JSON → 编辑器里改 → 导回去直接生效」的外部编辑通道，与 v1.3 只读导出成对。**两段式**：默认 `dryRun:true` 零写入只回 diff 计划 + checksum；`dryRun:false` 须带上该 checksum，服务端对**当前**库重新算一遍同一份计划、checksum 不一致即 409（无状态判据，不缓存上一次的计划）。`events`/`projections` 出现在请求体里（哪怕是 `null`）直接 400 并点名，**不许静默忽略**。删除需显式 `allowDelete:true`，缺省不删（`skippedDeletes` 供参考）；带 `allowDelete` 时按"创建→更新→删除（子先于父）"顺序逐条经 `guard.run_write` 执行，遇到既有级联保护（409）等业务校验失败即中止、不回滚已执行的动作。**actor 固定走人的路径**：本端点每条动作固定传 `body_actor="human"`，与 v1.6「actor 来源区分与高风险二次设防」判据原样复用——带 AI 凭据自报 human 同样被判成伪装而 403，import 没有比统一 CRUD 入口更宽松的口子。可写字段白名单直接反射自 `schemas.py` 的 `*Create`/`*Update` 模型（排除 `actor`），不重复维护第三份清单。**已知限制**：同一批不支持"父子两级都新建"（新建项目的 `zoneId`/新建任务的 `projectId` 必须指向已存在于库中的对象，无占位符 id 解析机制）。零新增 Mongo 集合，`events` 台账一个字节未动 |
| 2026-08-19 | 人类需求「计时器和网页前端都加个补登功能」 | v1.8：**契约 CR，实现待下一轮**——`timer.v1` 新增 `POST /api/core/timer/backfill`，给「完成了但没计时」的工作补一条历史记录。表单粒度＝任务+日期+开始时刻+时长（人类已定口径，不是"日期+时长"也不是"起止两端"）；入口 `ring`+`table` 两处（`gantt` 保持只读不加写入面）；必须挂具体任务，不允许只挂项目。信封 `data` 与 `stop()` 完全同形（`{durationSeconds, startAt}`），**零投影改动**——`proj_daily_stats` 归日取 `data.startAt`、`proj_current` 无日期维度，两者都分不出事件是实时计时组装的还是补登组装的；若实现时发现必须改投影，说明设计有错，应停下重新设计，不许绕过这条约束硬做。`source` 固定 `manual-backfill`（不是 `timer-backend`），与实时计时的防重键结构上不可能撞，且保留"表测出来的"与"人回忆的"两种证据强度的可分辨性；`dedupeKey` 为内容派生键（`backfill:<taskId>:<startAt 归一化为 UTC ISO>:<durationSeconds>`），归一化是防重生效的前提。7 条拒绝规则全部响亮报错：`taskId` 断链 404、`durationSeconds` 非正整数/超 86400 秒 400、`startAt` 缺时区偏移/无法解析 400、补登未来 400；**过旧的 `startAt` 不拦**。补登不碰 `timer_state`、允许与在跑计时墙钟重叠，不做先后判定。消费方表登记 `ring`（空闲/运行态均可点）与 `table`（任务行按钮，预填任务不可改）为待登记消费方 |
