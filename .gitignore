# Python 字节码。
#
# 必须忽略，而且不只是为了干净：改名/脱敏之后，旧的 .pyc 里**仍然嵌着旧字符串常量**，
# 于是真实数据会以二进制形式被带进发布产物——而文本扫描器扫不出二进制里的东西。
# 2026-09-17 匿名化时真撞见过这一下。
__pycache__/
*.py[cod]

# 安装器产出。默认组装用手写的 honeycomb/docker-compose.yml，
# 这个目录是 `install.sh add` 换模块组合时才生成的，不入仓。
honeycomb/generated/

# 本地环境
.env
