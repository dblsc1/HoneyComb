# HoneyComb 组装层 · 环境变量样例
#
# 用法：cp .env.example .env，把下面必填项填上，再 docker compose up -d。
# 本文件只应该进 Git 一份占位样例；真的 .env（带你的真口令）不要提交。

# nginx 监听地址。默认只绑回环（127.0.0.1），局域网/公网都探测不到。
# 要放到局域网自己改成 0.0.0.0:8800 之类——但那之前请先看 README 关于加门/加 TLS 的说明。
HONEYCOMB_BIND=127.0.0.1:8800

# 登录口令，必填。留空 = auth 服务直接拒绝启动（见 docker-compose.yml 里的 :? 校验），
# 而不是悄悄退回一个弱默认口令——这是故意的，别为了图方便给它塞默认值。
HONEYCOMB_PASSWORD=

HONEYCOMB_DB_NAME=honeycomb
HONEYCOMB_TZ=Asia/Shanghai

# cookie 的 Secure 标志。默认 true（只在 HTTPS 下发送）。
# 只有本机 HTTP 调试（没有 TLS）时才设 false，否则浏览器根本不会带上这个 cookie。
# 局域网/公网部署维持 true，并按 README 说明自己在前面加 TLS。
AUTH_COOKIE_SECURE=true

# compose 项目名。同一台机器上要并存两套 honeycomb 时改这个，
# 否则 `docker compose up` 会把另一套正在跑的容器改掉。
HONEYCOMB_PROJECT=honeycomb
